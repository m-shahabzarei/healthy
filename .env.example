# Optional hosted Supabase adapter. Leave these empty to keep the local demo.
NEXT_PUBLIC_SUPABASE_URL=
# Use either the legacy anon key below or the newer publishable key.
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Reserved for a future server-only migration; never expose this in NEXT_PUBLIC_*.
DATABASE_URL=
AUTH_SECRET=
